# Interface documentation

- [Per-surface completeness inventory](completeness.md)

The inventory covers the public website and the shared interface shipped in the Windows and Android packages. It records current behavior, gaps, and the evidence still required. An unimplemented row is an open delivery item, not an assertion that the feature is unnecessary.
